/**
 * Optional Vite config. This app primarily builds with Laravel Mix (webpack.mix.js).
 * Install Vite before using: npm install vite --save-dev
 *
 * @type {import('vite').UserConfig}
 */
export default {
    base: '/',
    server: {
        host: true,
    },
};
